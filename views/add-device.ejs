<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Add Device</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #cce7ff;
      display: flex;
      justify-content: center;
      align-items: center;
      height : 100vh;
      margin: 0;
    }
    .container {
      background-color: #fff;
      border-radius: 15px;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
      padding: 40px;
      text-align: center;
      max-width : 400px;
      width : 100%;
    }
    h1 {
      font-size: 2em;
      color: #003366;
      margin-bottom : 30px;
    }
    .button-container {
      display: flex;
      flex-direction: column;
      gap: 15px;
    }
    .action-button {
      display: inline-block;
      padding: 10px 25px;
      border-radius: 10px;
      background-color: #f1f1f1;
      border: 2px solid #bfbfbf;
      color: #333;
      font-size: 1em;
      cursor: pointer;
      transition: background-color 0.3s, transform 0.3s;
      text-decoration: none;
      font-weight: bold;
    }
    .action-button:hover {
      background-color: #007bff;
      color: #fff;
      transform: scale(1.05);
    }
    .action-button:active {
      background-color: #0056b3;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>Add Device</h1>
    <div class="button-container">
      <!-- Link to use QR Code for adding the device -->
      <a href="/upload-capture" class="action-button">Using QR code</a>

      <!-- Link to manually enter device ID -->
      <a href="/create-certificate" class="action-button">Manually Enter the Device ID</a>
    </div>
  </div>
</body>
</html>
